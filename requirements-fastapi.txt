# Зависимости оригинальной FastAPI-версии (app/main.py).
# НЕ нужны для Streamlit Cloud. Установка: pip install -r requirements-fastapi.txt
# pydantic нужен именно FastAPI (сам фреймворк на нём построен); Streamlit-часть
# от pydantic не зависит (DTO — на dataclasses).
-r requirements.txt
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9
pydantic>=2.9,<3
