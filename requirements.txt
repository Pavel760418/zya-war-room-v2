# Зависимости для Streamlit Cloud (Main file: streamlit_app.py).
# Держим набор минимальным — только то, что нужно Streamlit-приложению.
# FastAPI-зависимости вынесены в requirements-fastapi.txt, тестовые — в requirements-dev.txt.
streamlit==1.41.1
pandas==2.2.2
numpy>=1.26,<3           # прямой импорт в ingestion; совместим с pandas 2.2.x
openpyxl==3.1.5          # движок чтения/записи .xlsx для pandas
plotly==5.24.1
pydantic==2.9.2
