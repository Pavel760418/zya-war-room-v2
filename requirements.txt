# Зависимости для Streamlit Cloud (Main file: streamlit_app.py).
# Только то, что нужно Streamlit-приложению. БЕЗ pydantic: DTO переведены на
# dataclasses (app/models/schemas.py), поэтому Rust-сборка pydantic-core не нужна —
# деплой не падает на новых Python (3.13/3.14).
# Версии заданы диапазонами, чтобы Streamlit Cloud смог поставить готовые wheels
# под свою версию Python (см. README про рекомендуемый Python 3.13).
streamlit>=1.41,<2
pandas>=2.2.2,<3
numpy>=1.26,<3
openpyxl>=3.1
plotly>=5.24,<6
