# Зависимости для Streamlit (Cloud + LAN). Main file: streamlit_app.py.
# Без pydantic: DTO на dataclasses (app/models/schemas.py).
#
# SQL — единственный пользовательский путь. pymssql нужен для чтения 1С/MSSQL.
# На Streamlit Cloud хост 1С должен быть доступен по сети (VPN/туннель) и
# Secrets должны содержать DATABASE_URL (см. README / secrets.toml.example).
streamlit>=1.41,<2
pandas>=2.2.2,<3
numpy>=1.26,<3
openpyxl>=3.1
plotly>=5.24,<6
python-dotenv>=1.0,<2
pymssql>=2.2.11,<3
